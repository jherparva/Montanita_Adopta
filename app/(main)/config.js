import { dynamicRouteConfig } from '../../route-segment-config';

export const dynamic = dynamicRouteConfig.dynamic;
export const revalidate = dynamicRouteConfig.revalidate;
export const fetchCache = dynamicRouteConfig.fetchCache;
